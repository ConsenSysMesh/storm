version: '2'

services:
  storm:
    build: .
    tty: true
    stdin_open: true
    volumes:
      - "~/.storm:/root/.storm"
      # - "~/.docker/machine:/root/.docker/machine"  # Uncomment to use existing Docker machines
